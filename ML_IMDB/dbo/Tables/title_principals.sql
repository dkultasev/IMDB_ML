CREATE TABLE [dbo].[title_principals] (
    [tconst]     VARCHAR (MAX) NULL,
    [ordering]   VARCHAR (MAX) NULL,
    [nconst]     VARCHAR (MAX) NULL,
    [category]   VARCHAR (MAX) NULL,
    [job]        VARCHAR (MAX) NULL,
    [characters] VARCHAR (MAX) NULL
);

